'use strict';
import {namespace} from '../../app/util';

const ToggleFormulaAddModal = namespace('formulaManagement', 'ToggleFormulaAddModal')
const RenderFormulaList = namespace('formulaManagement', 'RenderFormulaList')
const GetFormulaList = namespace('formulaManagement', 'GetFormulaList')
const SubmitFormula = namespace('formulaManagement', 'SubmitFormula')
const UpdateState = namespace('formulaManagement', 'UpdateState')

export {ToggleFormulaAddModal, RenderFormulaList, SubmitFormula, UpdateState}
